<div>
  <div class="mb-8">
    <h1 class="text-2xl font-bold text-slate-900 mb-2">FieldWork Framework</h1>
    <p class="text-slate-600">The pedagogical architecture that ensures every experience is grounded in learning science.</p>
  </div>

  <!-- FieldWork Experiential Competencies -->
  <div class="card p-6 mb-6">
    <h2 class="text-lg font-semibold text-slate-900 mb-4 flex items-center gap-2">
      <div class="w-8 h-8 bg-primary-100 rounded-lg flex items-center justify-center">
        <svg class="w-4 h-4 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4M7.835 4.697a3.42 3.42 0 001.946-.806 3.42 3.42 0 014.438 0 3.42 3.42 0 001.946.806 3.42 3.42 0 013.138 3.138 3.42 3.42 0 00.806 1.946 3.42 3.42 0 010 4.438 3.42 3.42 0 00-.806 1.946 3.42 3.42 0 01-3.138 3.138 3.42 3.42 0 00-1.946.806 3.42 3.42 0 01-4.438 0 3.42 3.42 0 00-1.946-.806 3.42 3.42 0 01-3.138-3.138 3.42 3.42 0 00-.806-1.946 3.42 3.42 0 010-4.438 3.42 3.42 0 00.806-1.946 3.42 3.42 0 013.138-3.138z"></path>
        </svg>
      </div>
      FieldWork Experiential Learning Competencies
    </h2>
    <p class="text-sm text-slate-500 mb-4">These four competencies distinguish experiential learning from experiential activity:</p>

    <div class="grid sm:grid-cols-2 gap-3">
      <template x-for="comp in $store.app.framework?.experientialCompetencies" :key="comp.id">
        <div class="flex items-start gap-3 p-3 rounded-lg bg-primary-50 border border-primary-100">
          <div class="w-2 h-2 bg-primary-500 rounded-full mt-2 flex-shrink-0"></div>
          <div>
            <h3 class="font-medium text-slate-900 text-sm" x-text="comp.name"></h3>
            <p class="text-xs text-slate-500" x-text="comp.description"></p>
          </div>
        </div>
      </template>
    </div>
  </div>

  <!-- NACE Competencies -->
  <div class="card p-6 mb-6">
    <h2 class="text-lg font-semibold text-slate-900 mb-4 flex items-center gap-2">
      <div class="w-8 h-8 bg-emerald-100 rounded-lg flex items-center justify-center">
        <svg class="w-4 h-4 text-emerald-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 13.255A23.931 23.931 0 0112 15c-3.183 0-6.22-.62-9-1.745M16 6V4a2 2 0 00-2-2h-4a2 2 0 00-2 2v2m4 6h.01M5 20h14a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
        </svg>
      </div>
      NACE Career Readiness Competencies
    </h2>
    <p class="text-sm text-slate-500 mb-4">Industry-validated framework for professional skill development:</p>

    <!-- Required by FieldWork -->
    <div class="mb-4">
      <h3 class="text-xs font-semibold text-slate-500 uppercase tracking-wide mb-2">Required by FieldWork</h3>
      <div class="grid sm:grid-cols-2 gap-3">
        <template x-for="comp in $store.app.framework?.naceCompetencies?.required" :key="comp.id">
          <div class="flex items-start gap-3 p-3 rounded-lg bg-emerald-50 border border-emerald-100">
            <div class="w-2 h-2 bg-emerald-500 rounded-full mt-2 flex-shrink-0"></div>
            <div>
              <h3 class="font-medium text-slate-900 text-sm" x-text="comp.name"></h3>
              <p class="text-xs text-slate-500" x-text="comp.description"></p>
            </div>
          </div>
        </template>
      </div>
    </div>

    <!-- Optional (Institution Choice) -->
    <div>
      <h3 class="text-xs font-semibold text-slate-500 uppercase tracking-wide mb-2">Optional (Institution Choice)</h3>
      <div class="grid sm:grid-cols-2 gap-3">
        <template x-for="comp in $store.app.framework?.naceCompetencies?.optional" :key="comp.id">
          <div class="flex items-start gap-3 p-3 rounded-lg hover:bg-slate-50 transition-colors">
            <div class="w-2 h-2 bg-slate-300 rounded-full mt-2 flex-shrink-0"></div>
            <div>
              <h3 class="font-medium text-slate-900 text-sm" x-text="comp.name"></h3>
              <p class="text-xs text-slate-500" x-text="comp.description"></p>
            </div>
          </div>
        </template>
      </div>
    </div>
  </div>

  <!-- Assessment Architecture -->
  <div class="card p-6 mb-6">
    <h2 class="text-lg font-semibold text-slate-900 mb-4 flex items-center gap-2">
      <div class="w-8 h-8 bg-amber-100 rounded-lg flex items-center justify-center">
        <svg class="w-4 h-4 text-amber-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4"></path>
        </svg>
      </div>
      Assessment Architecture
    </h2>
    <p class="text-sm text-slate-500 mb-4">Recommended weighting model based on experiential learning research:</p>

    <div class="grid sm:grid-cols-2 gap-4 mb-4">
      <template x-for="item in $store.app.framework?.assessmentArchitecture" :key="item.id">
        <div class="flex items-start gap-3 p-3 rounded-lg bg-amber-50 border border-amber-100">
          <div class="w-12 h-12 bg-amber-500 rounded-lg flex items-center justify-center flex-shrink-0">
            <span class="text-white font-bold text-sm" x-text="item.weight + '%'"></span>
          </div>
          <div class="flex-1">
            <h3 class="font-medium text-slate-900 text-sm" x-text="item.name"></h3>
            <p class="text-xs text-slate-500" x-text="item.description"></p>
            <p class="text-xs text-amber-700 mt-1 italic" x-text="item.rationale"></p>
          </div>
        </div>
      </template>
    </div>

    <!-- Rationale note -->
    <div class="bg-slate-50 rounded-lg p-3 border border-slate-200">
      <p class="text-xs text-slate-600 leading-relaxed">
        <span class="font-medium text-slate-700">Rationale:</span> This weighting balances tangible work products (60%) with reflective practice and external validation (40%). Research shows reflection is essential for transforming experience into learning, while employer evaluation ensures assessment validity in authentic professional contexts.
      </p>
    </div>
  </div>

  <!-- Learning Strategies -->
  <div class="grid sm:grid-cols-2 gap-6">
    <!-- Kolb Cycle -->
    <div class="card p-6">
      <h2 class="text-lg font-semibold text-slate-900 mb-2" x-text="$store.app.framework?.learningStrategies?.kolbCycle?.name"></h2>
      <p class="text-sm text-slate-500 mb-4" x-text="$store.app.framework?.learningStrategies?.kolbCycle?.description"></p>

      <!-- Circular Cycle Diagram -->
      <div class="flex justify-center mb-4">
        <div class="relative w-64 h-64">
          <!-- Circular arrows SVG -->
          <svg class="absolute inset-0 w-full h-full" viewBox="0 0 200 200">
            <!-- Curved arrow paths connecting the phases -->
            <defs>
              <marker id="arrowhead" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto">
                <path d="M0,0 L6,3 L0,6 Z" fill="#94a3b8" />
              </marker>
            </defs>
            <!-- Top to Right -->
            <path d="M 115 35 Q 165 50 165 100" fill="none" stroke="#94a3b8" stroke-width="2" marker-end="url(#arrowhead)" />
            <!-- Right to Bottom -->
            <path d="M 165 115 Q 165 165 115 165" fill="none" stroke="#94a3b8" stroke-width="2" marker-end="url(#arrowhead)" />
            <!-- Bottom to Left -->
            <path d="M 85 165 Q 35 165 35 115" fill="none" stroke="#94a3b8" stroke-width="2" marker-end="url(#arrowhead)" />
            <!-- Left to Top -->
            <path d="M 35 85 Q 35 35 85 35" fill="none" stroke="#94a3b8" stroke-width="2" marker-end="url(#arrowhead)" />
          </svg>

          <!-- Phase nodes positioned in a circle -->
          <!-- Top: Experiencing (blue) -->
          <div class="absolute top-0 left-1/2 -translate-x-1/2 text-center">
            <span class="badge badge-blue text-xs">Experiencing</span>
            <p class="text-xs text-slate-500 mt-1 w-20 mx-auto leading-tight">Doing the actual work</p>
          </div>

          <!-- Right: Reflecting (green) -->
          <div class="absolute top-1/2 right-0 -translate-y-1/2 text-center">
            <span class="badge badge-green text-xs">Reflecting</span>
            <p class="text-xs text-slate-500 mt-1 w-20 mx-auto leading-tight">Processing what happened</p>
          </div>

          <!-- Bottom: Thinking (purple) -->
          <div class="absolute bottom-0 left-1/2 -translate-x-1/2 text-center">
            <span class="badge badge-purple text-xs">Thinking</span>
            <p class="text-xs text-slate-500 mt-1 w-20 mx-auto leading-tight">Connecting to theory</p>
          </div>

          <!-- Left: Acting (amber) -->
          <div class="absolute top-1/2 left-0 -translate-y-1/2 text-center">
            <span class="badge badge-amber text-xs">Acting</span>
            <p class="text-xs text-slate-500 mt-1 w-20 mx-auto leading-tight">Applying insights</p>
          </div>
        </div>
      </div>

      <!-- Explanatory note -->
      <div class="bg-slate-50 rounded-lg p-3 border border-slate-200">
        <p class="text-xs text-slate-600 leading-relaxed">
          <span class="font-medium text-slate-700">Note:</span> Learners may enter the cycle at any phase depending on their situation. This is a continuous cycle that repeats week to week—each iteration builds on the last rather than following a linear sequence with a fixed beginning and end.
        </p>
        <a href="https://experientiallearninginstitute.org/what-is-experiential-learning/" target="_blank" rel="noopener noreferrer" class="inline-flex items-center gap-1 text-xs text-primary-600 hover:text-primary-700 mt-2">
          Learn more about experiential learning
          <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path>
          </svg>
        </a>
      </div>
    </div>

    <!-- DEAL Model -->
    <div class="card p-6">
      <h2 class="text-lg font-semibold text-slate-900 mb-2" x-text="$store.app.framework?.learningStrategies?.dealModel?.name"></h2>
      <p class="text-sm text-slate-500 mb-4" x-text="$store.app.framework?.learningStrategies?.dealModel?.description"></p>

      <div class="space-y-3">
        <template x-for="component in $store.app.framework?.learningStrategies?.dealModel?.components" :key="component.id">
          <div class="bg-violet-50 rounded-lg p-3 border border-violet-100">
            <h3 class="font-medium text-violet-900 text-sm" x-text="component.name"></h3>
            <p class="text-xs text-violet-700" x-text="component.prompt"></p>
          </div>
        </template>
      </div>

      <a href="https://pressbooks.pub/etsu/chapter/the-deal-model-metacognitive-reflection-in-service-learning/" target="_blank" rel="noopener noreferrer" class="inline-flex items-center gap-1 text-xs text-violet-600 hover:text-violet-700 mt-4">
        Learn more about the DEAL model
        <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path>
        </svg>
      </a>
    </div>
  </div>
</div>
